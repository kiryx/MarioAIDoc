\chapter{Podsumowanie}
\begin{par}
	Podczas implementacji systemu wynik³y problemy dotycz¹ce wielow¹tkowoœci w jêzyku java, a dok³adnie wspó³dzielenie zasobów przez w¹tek gry, oraz w¹tek zwi¹zany z wyœwietlaniem interfejsu u¿ytkownika. Podczas pracy nad systemem du¿ym problemem by³o czêste pojawianie siê wyj¹tku ``ConcurrentModificationException'' dotycz¹cego jednoczej modyfikacji listy elementów gry przez dwa w¹tki. Domyœlnym rozwi¹zaniem jest umieszczenie modyfikacji listy w bloku synchronized{ }, co zmniejszy³o czêstotliwoœæ wystêpowania wyj¹tku, jednak dopiero zastosowanie zmiennej typu mutex rozwi¹za³o ca³kowicie problem. Przypuszcza siê i¿ mo¿e byæ to pewna w³aœciwoœæ biblioteki Swing która w po³¹czeniu z pewnymi b³êdami projektowymi po stronie implementacji system, sprawia³a problem.
\end{par}
\begin{par}
	Przy uruchamianiu aplikacji na wolniejszych komputerach mo¿na by³o zauwa¿yæ pewien spadek p³ynnoœci gry. Problem ten mo¿na by³oby rozwi¹zaæ korzystaj¹c z gotowych bibliotek graficznych (np. OpenGL), które dzia³aj¹ szybciej ni¿ w³asna implementacja warstwy graficznej. Innym usprawnieniem mog³oby byæ u¿ycie biblioteki SWT zamiast biblioteki Swing, dziêki czemu wygl¹d aplikacji lepiej pasowa³by do danego systemu operacyjnego - aplikacje okienkowe napisane w bibliotece SWT przypominaj¹ wygl¹dem standardowe aplikacje systemu. Jeœli chodzi o sam jêzyk programowania to Java w zupe³noœci spe³nia wymagania systemu.
\end{par}
\begin{par}
	Sam system genetyczny daje doœæ dobre wyniki na prostych mapach (np. ukierunkowanych). Du¿o gorzej radzi sobie z logik¹ wymagaj¹c¹ poruszania siê w 4 kierunkach (np. LogicLabirynth). Usprawnieniem systemu mog³oby byæ wykorzystanie algorytmów przeszukuj¹cych przestrzeñ rozwi¹zañ (np. algorytm przeszukuj¹cy A*) wraz z algorytmem genetycznym. Sam algorytm genetyczny znajduje rozwi¹zanie za pomoc¹ standardowego podejœcia, jednak jeœli celem systemu by³oby najszybsze rozwi¹zanie danej gry, algorytm przeszukuj¹cy z pewn¹ heurystyk¹ mo¿e okazaæ siê lepszym podejœciem. Du¿¹ zalet¹ systemu jest jego elastycznoœæ - mo¿liwoœæ dodawania w³asnych map oraz logik do systemu. Z perspektywy czasu autor systemu jest zadowolony z otrzymanych rezultatów, jednak przy ponownej próbie implementacji skorzysta³by z innych narzêdzi i byæ mo¿e nieco innego podejœcia algorytmicznego do problemu.
\end{par}
